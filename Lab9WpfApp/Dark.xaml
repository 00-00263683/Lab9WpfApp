<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="WindowsStyle">
        <Setter Property="Control.Background" Value="#FF6C70B8"/>        
    </Style>
    <Style x:Key="ToolbartrayStyle" BasedOn="{StaticResource WindowsStyle}">
        <Setter Property="ToolBarTray.Background" Value="#FF9092BD"/>
    </Style>   
    <Style x:Key="RadiobuttonStyle" BasedOn="{StaticResource WindowsStyle}">        
        <Setter Property="ToolBar.Foreground" Value="White"/>
    </Style>
    <Style x:Key="ComboboxStyle" BasedOn="{StaticResource WindowsStyle}">
        <Setter Property="ComboBox.Background" Value="#FF898EAE"/>
        <Setter Property="ComboBox.BorderBrush" Value="#FF535353"/>
        <Setter Property="ComboBox.Foreground" Value="White"/>
    </Style>
    <Style x:Key="TextBlockStyle" BasedOn="{StaticResource WindowsStyle}">            
    </Style>
    <Style x:Key="MenuitemStyle" BasedOn="{StaticResource WindowsStyle}">
        <Setter Property="MenuItem.Foreground" Value="White"/>
        <Setter Property="MenuItem.BorderBrush" Value="#FF898EAE"/>       
    </Style>
    <Style x:Key="MenuStyle" BasedOn="{StaticResource WindowsStyle}">
        <Setter Property="Menu.Foreground" Value="White"/>
        <Setter Property="Menu.BorderBrush" Value="#FF898EAE"/>
    </Style>
    <Style x:Key="TextblockStyle" BasedOn="{StaticResource WindowsStyle}">
        <Setter Property="TextBlock.Foreground" Value="White"/>
        <Setter Property="TextBlock.Background" Value="#FF898EAE"/>
    </Style>
</ResourceDictionary>